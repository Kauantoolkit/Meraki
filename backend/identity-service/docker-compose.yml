# ─── identity-service — deploy independente ────────────────────────────────
# Sobe o serviço + seu PostgreSQL próprio.
# RabbitMQ é externo: configure RABBITMQ_URL no .env apontando para o servidor
# de infra (ver backend/docker-compose.infra.yml).
#
# Uso:
#   cp .env.example .env   # preencha os valores
#   docker compose up -d

services:
  identity-service:
    build: .
    container_name: meraki-identity-service
    ports:
      - '${PORT:-3001}:${PORT:-3001}'
    environment:
      NODE_ENV: ${NODE_ENV:-production}
      PORT: ${PORT:-3001}
      DB_HOST: identity-db
      DB_PORT: 5432
      DB_NAME: ${DB_NAME:-identity_db}
      DB_USER: ${DB_USER}
      DB_PASS: ${DB_PASS}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      RABBITMQ_URL: ${RABBITMQ_URL}
    depends_on:
      identity-db:
        condition: service_healthy
    restart: unless-stopped

  identity-db:
    image: postgres:15-alpine
    container_name: meraki-identity-db
    environment:
      POSTGRES_DB: ${DB_NAME:-identity_db}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASS}
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-postgres}']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  db-data:

export enum ProjectRoutingKey {
  PROJECT_CREATED = 'project.created',
  PROJECT_COMPLETED = 'project.completed',
  MILESTONE_CREATED = 'milestone.created',
  MILESTONE_UPDATED = 'milestone.updated',
}

PORT=3002
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5433
DB_NAME=project_db
DB_USER=postgres
DB_PASS=postgres

# JWT (mesmo secret do identity-service — só para validar tokens)
JWT_SECRET=meraki-jwt-secret

# RabbitMQ
RABBITMQ_URL=amqp://meraki:meraki@localhost:5672
